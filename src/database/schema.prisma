generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Configuration {
  id        Int    @id @default(autoincrement())
  email     String @unique
  key       String
  zone      String
  record    String
  frequency Int
}
